apiVersion: apps/v1
kind: Deployment
metadata:
  name: stones-web
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: stones-web
    spec:
      containers:
        - name: stones-web
          image: gcr.io/PROJECT_ID/IMAGE:TAG
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 250m
            limits:
              cpu: 1000m
  selector:
    matchLabels:
      app: stones-web
